extends layout

block pageTitle
    title= title || 'Contacts Book'

block content
    //- if htmlWebpackPlugin
    //-     h1= htmlWebpackPlugin.options.title
    //- else
    //-     h1= title
    //- p Welcome to the most fucking awesome AddressBook app
    //- p Made with #[pre <3]
    .body-wrapper
    //- a(href="/signout")
        button.btn.btn-outline-primary.btn-sm.rounded-0 Sign out

block closeHead
    link(rel="dns-prefetch" href="https://restcountries.eu")
    link(rel="stylesheet" href="/App.css")

block closeBody
    script(nonce=nonce src="/App.js")
